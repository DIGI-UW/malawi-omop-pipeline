[project]
name = "omop-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pymysql>=1.1.1",
    "sqlmesh>=0.167.1",
]

[dependency-groups]
dev = [
    "ruff>=0.11.2",
    "sqlmesh[web]>=0.167.1",
]
