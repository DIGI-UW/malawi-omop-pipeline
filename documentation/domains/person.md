# Person

This is the most basic table that contains one record for each unique person or patient in the dataset. For our purposes, there is a one-to-one mapping from OHDL's `patient` table to the OMOP `person` table.
